---
const links = [
  { href: "#sobre", label: "Sobre" },
  { href: "#skills", label: "Skills" },
  { href: "#projetos", label: "Projetos" },
  { href: "#contato", label: "Contato" },
];
---

<header class="sticky top-0 z-50 border-b border-zinc-800 bg-zinc-950/80 backdrop-blur">
  <div class="mx-auto flex max-w-5xl items-center justify-between px-4 py-3">
    <a href="/" class="font-semibold tracking-tight">Carlos</a>

    <nav class="flex gap-4 text-sm text-zinc-300">
      {links.map((l) => (
        <a class="hover:text-white" href={l.href}>
          {l.label}
        </a>
      ))}
    </nav>
  </div>
</header>
